device: #Überall erstmal den richten Namen auf Englisch schreiben, dann die Erklärung
  filesystem:
    maxCharsPerFile: "Max amount of chars inside of a file."
    maxElementsPerLevel: "Max amount of files and folders per filesystem-leve."
    fileNeedSpacePerHundredChars: "Space needed per file per started hundred characters."
    maxElementsOverall: "Max amount of elements a player can create on one computer."
oberflaeche:
  desktop:
    maxIconsOnDesktop: "Max amount of icons the player can store on the desktop."
userAccount:
  blabla:
services:
  network:
    blabla:

  hacking:
    blabla:
  
